services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8081:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    volumes:
      # persist leaderboard data across container restarts
      - ./backend/backend/data:/app/backend/data
